geom_col(aes(fill=padj<0.05)) +
coord_flip() +
labs(x="Pathway", y="Normalized Enrichment Score",
title="Hallmark pathways NES from GSEA") +
theme_minimal()
plotEnrichment(pathways.allergy[["HP_ALLERGY"]],
ranks)
knitr::opts_chunk$set(echo = TRUE)
res <- read_csv("..\\..\\data\\results\\differential_gene_expression\\activ_pers_vs_resolved_baseline_genomewide.csv")
res
# Convert GeneID column values from numeric to character
res$GeneID <- as.character(res$GeneID)
# Map Entrez gene IDs to gene symbols
id_to_symbol <- AnnotationDbi::select(
org.Hs.eg.db, key=res$GeneID,
columns=c("SYMBOL","GENENAME"), keytype="ENTREZID"
)
id_to_symbol
study_gene_list <- read.table("..\\..\\data\\raw\\study_gene_list.txt")
View(study_gene_list)
study_gene_list_symb
# study_gene_list_symb
source("~/.active-rstudio-document", echo=TRUE)
# Convert GeneID column values from numeric to character
key=study_gene_list$V1 <- as.character(key=study_gene_list$V1)
# Convert GeneID column values from numeric to character
study_gene_list$V1 <- as.character(study_gene_list$V1)
# study_gene_list_symb
# Convert GeneID column values from numeric to character
study_gene_list$V1 <- as.character(study_gene_list$V1)
knitr::opts_chunk$set(echo = TRUE)
meth_data <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 65, nrows = 100000) # file path: ..\\..\\ goes up two folders
# skip the first 65 rows
# update to read in all rows in chunks!!
meth_data
# library("data.table")
# fread("C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt", data.table=FALSE, showProgress=TRUE, sep=" ")
meth_data_format <- meth_data
colnames(meth_data_format) <- meth_data[1, ]
meth_data_format
?read.table
meth_data_format <- meth_data
colnames(meth_data_format) <- meth_data[1, ]
rownames(meth_data_format) <- meth_data[, 1]
meth_data_format
View(meth_data_format)
meth_data_format <- meth_data
colnames(meth_data_format) <- meth_data[1, ] # make sample names the column names
rownames(meth_data_format) <- meth_data[, 1] # make probe ID the row names
meth_data_format <- meth_data_format[-1, ] # delete first row
meth_data_format <- meth_data_format[, -1] # delete first column
meth_data_format
# Save this chunk methylation data to csv: probes 1-100,000
write.csv(as.data.frame(meth_data_format),
file="..\\..\\data\\processed\\methylation_data_1.csv")
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 29, nrows = 1)
sample_id
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels
View(sample_labels)
View(sample_id)
sample_info_df <- data.frame(
sample_id,
sample_labels
)
sample_info_df
sample_info_df <- data.frame(
sample_id[1,],
sample_labels[1,]
)
sample_info_df
sample_info_df <- rbind(
sample_id,
sample_labels
)
sample_info_df
sample_info_df <- rbind(
sample_id,
sample_labels
)
sample_info_df <- sample_info_df[,-1]
sample_info_df
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels
sample_allerg_status <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 40, nrows = 1)
sample_allerg_status
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1]
sample_info_df
View(sample_info_df)
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1]
# Name rows
rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
sample_info_df
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1]
sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1]
sample_info_df
sample_info_df <- t(sample_info_df)
sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1]
sample_info_df
sample_info_df <- transpose(sample_info_df)
View(sample_info_df)
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1]
sample_info_df
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
View(test_coldata_df)
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df
sample_info_df <- sample_info_df[,-1]
sample_info_df
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
colnames(sample_info_df) <- meth_data[1, ] # make sample names the column names
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1] # remove unneeded column
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
rownames(sample_info_df) <- meth_data[, 1] # make sample IDs the row names
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1] # remove unneeded column
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
# colnames(sample_info_df) <- meth_data[1, ] # make sample names the column names
#
# meth_data_format <- meth_data_format[-1, ] # delete first row
# meth_data_format <- meth_data_format[, -1] # delete first column
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1] # remove unneeded column
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
# rownames(sample_info_df) <- c("sample ID", "allergy phenotype", "age", "stimulation")
#
# sample_info_df
rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
sample_info_df <- sample_info_df[, -1] # delete first column
# colnames(sample_info_df) <- meth_data[1, ] # make sample names the column names
#
# meth_data_format <- meth_data_format[-1, ] # delete first row
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1] # remove unneeded column
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
#
# sample_info_df
rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
sample_info_df <- sample_info_df[, -1] # delete first column
colnames(sample_info_df) <- c("allergy phenotype", "age", "stimulation")
#
# meth_data_format <- meth_data_format[-1, ] # delete first row
View(sample_info_df)
sample_info_df <- rbind(
sample_id,
sample_labels,
sample_allerg_status
)
sample_info_df <- sample_info_df[,-1] # remove unneeded column
sample_info_df <- t(sample_info_df)
# sample_info_df
# # Name rows
#
# sample_info_df
rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
sample_info_df <- sample_info_df[, -1] # delete first column
colnames(sample_info_df) <- c("allergy phenotype", "age", "stimulation")
#
# meth_data_format <- meth_data_format[-1, ] # delete first row
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
# Create new metadata table with only samples from the baseline timepoint (age 1)
sample_info_df
baseline_samples <- filter(sample_info_df, stimulation == "stimulation: 1")
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id <- t(sample_id)
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels <- t(sample_labels)
sample_allerg_status <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 40, nrows = 1)
sample_allerg_status <- t(sample_allerg_status)
View(sample_id)
View(sample_allerg_status)
View(sample_allerg_status)
View(sample_id)
View(sample_labels)
View(sample_info_df)
View(sample_id)
View(sample_labels)
View(sample_allerg_status)
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id <- t(sample_id)
sample_id <- sample_id[-1,]
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels <- t(sample_labels)
sample_allerg_status <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 40, nrows = 1)
sample_allerg_status <- t(sample_allerg_status)
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id <- t(sample_id)
sample_id <- sample_id[-1, ] # remove first row
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels <- t(sample_labels)
sample_allerg_status <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 40, nrows = 1)
sample_allerg_status <- t(sample_allerg_status)
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id <- t(sample_id)
# sample_id <- sample_id[-1, ] # remove first row
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels <- t(sample_labels)
sample_allerg_status <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 40, nrows = 1)
sample_allerg_status <- t(sample_allerg_status)
View(sample_id)
View(sample_labels)
View(sample_labels)
View(sample_allerg_status)
View(sample_labels)
View(sample_allerg_status)
# Read in sample IDs and allergy status
sample_id <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 28, nrows = 1)
sample_id <- t(sample_id)
# sample_id <- sample_id[-1, ] # remove first row
sample_labels <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 36, nrows = 2)
sample_labels <- t(sample_labels)
sample_act_status <- read.table(file = 'C:\\Users\\Amanda Chan\\Documents\\440 Project\\Dataset\\Methylation\\GSE114134_series_matrix.txt', sep = "", skip = 40, nrows = 1)
sample_act_status <- t(sample_act_status)
View(sample_act_status)
sample_info_df <- data.frame(
age = as.vector(sample_labels[,2]),
allergy_status = as.vector(sample_labels[,1]),
activation_status = as.vector(sample_act_status[,1]),
row.names = row.names(sample_id[1,])
)
sample_info_df
# sample_info_df <- rbind(
#   sample_id,
#   sample_labels,
#   sample_allerg_status
# )
# sample_info_df <- sample_info_df[,-1] # remove unneeded column
#
# sample_info_df <- t(sample_info_df)
#
# rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
# sample_info_df <- sample_info_df[, -1] # delete first column
#
# colnames(sample_info_df) <- c("allergy phenotype", "age", "stimulation")
sample_info_df <- data.frame(
age = as.vector(sample_labels[,2]),
allergy_status = as.vector(sample_labels[,1]),
activation_status = as.vector(sample_act_status[,1]),
row.names = sample_id[1,]
)
sample_info_df <- data.frame(
age = as.vector(sample_labels[,2]),
allergy_status = as.vector(sample_labels[,1]),
activation_status = as.vector(sample_act_status[,1]),
row.names = sample_id[,1]
)
sample_info_df
# sample_info_df <- rbind(
#   sample_id,
#   sample_labels,
#   sample_allerg_status
# )
# sample_info_df <- sample_info_df[,-1] # remove unneeded column
#
# sample_info_df <- t(sample_info_df)
#
# rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
# sample_info_df <- sample_info_df[, -1] # delete first column
#
# colnames(sample_info_df) <- c("allergy phenotype", "age", "stimulation")
sample_info_df <- data.frame(
age = as.vector(sample_labels[,2]),
allergy_status = as.vector(sample_labels[,1]),
activation_status = as.vector(sample_act_status[,1]),
row.names = sample_id[,1]
)
sample_info_df <- sample_info_df[-1, ] # remove first row
# sample_info_df <- rbind(
#   sample_id,
#   sample_labels,
#   sample_allerg_status
# )
# sample_info_df <- sample_info_df[,-1] # remove unneeded column
#
# sample_info_df <- t(sample_info_df)
#
# rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
# sample_info_df <- sample_info_df[, -1] # delete first column
#
# colnames(sample_info_df) <- c("allergy phenotype", "age", "stimulation")
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
sample_info_df <- data.frame(
age = as.vector(sample_labels[,2]),
allergy_status = as.vector(sample_labels[,1]),
activation_status = as.vector(sample_act_status[,1]),
row.names = sample_id[,1]
)
sample_info_df <- sample_info_df[-1, ] # remove first row
# sample_info_df <- rbind(
#   sample_id,
#   sample_labels,
#   sample_allerg_status
# )
# sample_info_df <- sample_info_df[,-1] # remove unneeded column
#
# sample_info_df <- t(sample_info_df)
#
# rownames(sample_info_df) <- sample_info_df[, 1] # make sample IDs the row names
# sample_info_df <- sample_info_df[, -1] # delete first column
#
# colnames(sample_info_df) <- c("allergy phenotype", "age", "stimulation")
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
library(dplyr)
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
baseline_samples
# Of the baseline timepoint data, take the activated datapoints
baseline_samples_act <- filter(baseline_samples, stimulation == "stimulation: 1")
library(dplyr)
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
baseline_samples
# Of the baseline timepoint data, take the activated datapoints
baseline_samples_act <- filter(baseline_samples, activation_status == "stimulation: 1")
baseline_samples_act
# Remove the control datapoints
# baseline_samples_act_allerg <- filter(baseline_samples_act, allergy phenotype == "allergic")
library(dplyr)
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
baseline_samples
# Of the baseline timepoint data, take the activated datapoints
baseline_samples_act <- filter(baseline_samples, activation_status == "stimulation: 1")
baseline_samples_act
# Separate into transient and persistent allergics
baseline_samples_act_trans <- filter(baseline_samples_act, allergy_status == "allergy_phenotype: transient")
baseline_samples_act_pers <- filter(baseline_samples_act, allergy phenotype == "allergy_phenotype: persistent")
library(dplyr)
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
baseline_samples
# Of the baseline timepoint data, take the activated datapoints
baseline_samples_act <- filter(baseline_samples, activation_status == "stimulation: 1")
baseline_samples_act
# Separate into transient and persistent allergics
baseline_samples_act_trans <- filter(baseline_samples_act, allergy_status == "allergy_phenotype: transient")
baseline_samples_act_pers <- filter(baseline_samples_act, allergy_status == "allergy_phenotype: persistent")
View(baseline_samples_act_pers)
View(baseline_samples_act_trans)
View(baseline_samples_act)
library(dplyr)
# Create new metadata table with only samples from the baseline timepoint (age 1)
baseline_samples <- filter(sample_info_df, age == 'age: 1')
baseline_samples
# Of the baseline timepoint data, take the activated datapoints
baseline_samples_act <- filter(baseline_samples, activation_status == "stimulation: 1")
baseline_samples_act
# Separate into transient and persistent allergics
baseline_samples_act_trans <- filter(baseline_samples_act, allergy_status == "allergy_phenotype: transient")
baseline_samples_act_pers <- filter(baseline_samples_act, allergy_status == "allergy_phenotype: persistent")
baseline_samples_act_ctrl <- filter(baseline_samples_act, allergy_status == "allergy_phenotype: nonallergic")
View(baseline_samples_act_ctrl)
View(baseline_samples_act_trans)
# Get sample names
label_baseline_samples_act_trans <- row.names(baseline_samples_act_trans)
label_baseline_samples_act_pers <- row.names(baseline_samples_act_pers)
# Create new dataframe with only data samples from the activated allergic samples at baseline timepoint
# follow_up_countData <- countData[,c("GeneID", sample_label_follow_up)]
# baseline_act_allerg_countData <- countData[,c("GeneID", sample_label_baseline_act_allerg)]
# Get sample names
label_baseline_samples_act_trans <- row.names(baseline_samples_act_trans)
label_baseline_samples_act_pers <- row.names(baseline_samples_act_pers)
label_baseline_samples_act_trans
label_baseline_samples_act_pers
# Create new dataframe with only data samples from the activated allergic samples at baseline timepoint
# follow_up_countData <- countData[,c("GeneID", sample_label_follow_up)]
# baseline_act_allerg_countData <- countData[,c("GeneID", sample_label_baseline_act_allerg)]
source("~/.active-rstudio-document", echo=TRUE)
View(meth_data_format)
View(meth_data)
View(meth_data_format)
View(countData)
# Get sample names
label_baseline_samples_act_trans <- row.names(baseline_samples_act_trans)
label_baseline_samples_act_pers <- row.names(baseline_samples_act_pers)
label_baseline_samples_act_trans
label_baseline_samples_act_pers
# Create new dataframe with only data samples from the activated allergic samples at baseline timepoint
# follow_up_countData <- countData[,c("GeneID", sample_label_follow_up)]
baseline_act_trans_meth_data <- meth_data_format[, (label_baseline_samples_act_trans)]
View(baseline_samples_act_trans)
View(baseline_samples_act_trans)
# Get sample names
label_baseline_samples_act_trans <- row.names(baseline_samples_act_trans)
label_baseline_samples_act_pers <- row.names(baseline_samples_act_pers)
label_baseline_samples_act_trans
label_baseline_samples_act_pers
# Create new dataframe with only data samples from the activated allergic samples at baseline timepoint
# follow_up_countData <- countData[,c("GeneID", sample_label_follow_up)]
baseline_act_trans_meth_data <- meth_data_format[, (label_baseline_samples_act_trans)]
baseline_act_pers_meth_data <- meth_data_format[, (label_baseline_samples_act_pers)]
View(baseline_act_pers_meth_data)
# Get sample names
label_baseline_samples_act_trans <- row.names(baseline_samples_act_trans)
label_baseline_samples_act_pers <- row.names(baseline_samples_act_pers)
label_baseline_samples_act_trans
label_baseline_samples_act_pers
# Create new dataframe with only data samples from the activated allergic samples at baseline timepoint
# follow_up_countData <- countData[,c("GeneID", sample_label_follow_up)]
baseline_act_trans_meth_data <- meth_data_format[, (label_baseline_samples_act_trans)]
baseline_act_pers_meth_data <- meth_data_format[, (label_baseline_samples_act_pers)]
baseline_act_meth_data <- cbind(baseline_act_trans_meth_data, baseline_act_pers_meth_data)
View(baseline_act_meth_data)
